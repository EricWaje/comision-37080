import React from 'react';
import Form from '../Form/Form';

const Cart = () => {
    return (
        <div style={{ display: 'flex', justifyContent: 'center' }}>
            <Form />
        </div>
    );
};

export default Cart;
